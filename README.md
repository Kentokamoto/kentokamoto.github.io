# kentokamoto.github.io

# Usage

Because this uses jekyll and tailwindcss for static site generation, there are
two commands that need to be run

```
jekyll serve
```

```
npx tailwindcss -i styles/style.css -o ./dist/output.css --watch

```
